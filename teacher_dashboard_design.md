# 2학년 야간자율학습 출석 프로그램 - 담임 교사용 대시보드 설계서

본 문서는 야간자율학습(야자) 출석체크 웹 프로그램에 새롭게 추가될 **'담임 교사 조회용 대시보드(원페이퍼 리포트)'**의 기능 및 구조 설계를 기술합니다.

---

## 1. 개요
* **목적**: 담임 교사가 모바일 및 PC에서 로그인 절차 없이 자신의 반 야자 참여 학생들의 일일 출결 현황과 결석 사유(비고)를 즉시 파악하고, A4 용지 1장(원페이퍼)의 PDF로 저장 및 출력할 수 있도록 지원합니다.
* **주요 변경 사항**:
  * 웹앱(`index.html`)에 **[감독 교사 모드]**와 **[담임 교사 모드]** 선택 탭 신설
  * 담임 교사 모드 전용 '요약 카드' 및 '출결 일람표(원페이퍼 리포트)' UI 구현
  * Google Apps Script(GAS)에 특정 반의 전체 명단과 실시간 출결 정보를 병합하여 제공하는 전용 API(`action=getClassReport`) 추가

---

## 2. UI/UX 화면 구성
```
+---------------------------------------------------------+
|                [야자 자리 배치 & 출결 프로그램]                 |
|       +-------------------+  +-------------------+      |
|       |    감독 교사 모드    |  |  ★ 담임 교사 모드  |      |
|       +-------------------+  +-------------------+      |
+---------------------------------------------------------+
| [ 필터 설정 ]                                            |
| 날짜 선택: [ 2026. 05. 21 ]    반 선택: [ 2-3반 v ]      |
+---------------------------------------------------------+
| [ 출결 요약 ]                                            |
| +-------------------+ +-------------------+ +---------+ |
| |      총 대상자    | |       출석인원    | | 결석인원| |
| |        15명       | |        12명       | |   3명   | |
| +-------------------+ +-------------------+ +---------+ |
+---------------------------------------------------------+
| [ 출결 일람표 (원페이퍼 출력 영역) ]     [📄 PDF 인쇄하기] |
|                                                         |
| 번호 | 학번  | 이름  | 1교시 출결 | 2교시 출결 | 결석사유(비고) |
| ------------------------------------------------------- |
|  1   | 20104 | 김예나 |    출석    |    결석   |  학원보강  |
|  2   | 20111 | 배수현 |    결석    |    결석   |  질병조퇴  |
|  3   | 20119 | 정민기 |    출석    |    출석   |            |
| ...                                                     |
+---------------------------------------------------------+
```

### 주요 UI 특징:
1. **모바일 최적화 (반응형)**: 스마트폰 화면에서는 테이블이 가로 스크롤 되거나 카드 형태로 보기 쉽게 축소 렌더링됩니다.
2. **시각적 가시성**: 출석 상태는 파란색 계열, 결석 상태는 빨간색 계열의 둥근 배지 형태로 강조 처리하여 한눈에 식별 가능하도록 합니다.
3. **인쇄 최적화 (CSS media query `@print`)**: PDF 저장 혹은 인쇄 실행 시, 브라우저 상단의 모드 전환 탭, 날짜/반 필터 드롭다운, 인쇄 버튼 등 불필요한 컨트롤 요소를 숨기고 **[출결 요약]**과 **[출결 일람표]** 표만 A4 한 페이지에 단정하게 담기도록 스타일을 제어합니다.

---

## 3. 기능 정의

### ① 모드 전환 및 세션 보존
* 웹앱 첫 화면에서 사용자가 원하는 모드를 선택할 수 있으며, 마지막에 선택한 모드 정보는 `localStorage`에 저장되어 재접속 시 유지됩니다.
* **담임 교사 모드**는 로그인(비밀번호나 교사 명단 대조) 절차를 거치지 않고 바로 조회가 가능합니다.

### ② 실시간 출결 현황 조회
* 조회 날짜와 학반을 변경할 때마다 비동기 Fetch 요청을 통해 GAS API에서 해당 반 학생들의 명단과 출결 상태를 결합한 데이터를 새로고침 없이 즉시 가져옵니다.

### ③ PDF 내보내기 (`html2pdf.js` 활용)
* 화면에 출력된 일람표 영역만 A4 규격(`portrait` 세로 방향)으로 변환하여 `야자출결현황_2-3반_2026-05-21.pdf` 형태로 브라우저에서 다운로드합니다.

---

## 4. 데이터 연동 규격 (GAS API)

### 요청 (Request)
* **호출 URL**: 기존 GAS 웹 앱 URL
* **HTTP Method**: `GET`
* **Query Parameters**:
  * `action=getClassReport`
  * `date=YYYY. M. D.` (예: `2026. 5. 21.`)
  * `room=2-3` (조회할 반 정보)

### 응답 (Response - JSON)
해당 반에 배정된 모든 학생의 학번 순 목록과 당일 `Attendance_Log` 시트 상의 출결 기록을 매핑하여 반환합니다.
```json
[
  {
    "id": "20104",
    "name": "김예나",
    "p1_status": "출석",
    "p1_remark": "",
    "p2_status": "결석",
    "p2_remark": "학원보강",
    "p1_supervisor": "유연주",
    "p2_supervisor": "유연주"
  },
  {
    "id": "20111",
    "name": "배수현",
    "p1_status": "결석",
    "p1_remark": "급체",
    "p2_status": "결석",
    "p2_remark": "급체",
    "p1_supervisor": "유연주",
    "p2_supervisor": "유연주"
  }
]
```

---

## 5. 소스 코드 수정 방안

### ① `index.html` 마크업 변경
* 기존 `#loginOverlay` 및 `#mainApp` 레이아웃 구조 상단에 탭 네비게이션을 삽입합니다.
* 담임 교사용 뷰 영역인 `#teacherDashboardView` 레이아웃을 생성합니다.

### ② `index.html` 스크립트 보완
* `changeViewMode(mode)` 함수를 만들어 `active` 클래스를 토글하고 화면의 display 속성을 제어합니다.
* `fetchClassReport(date, room)` 함수를 추가하여 새롭게 구현될 GAS API와 통신하고 테이블 요소를 동적으로 생성해 줍니다.

### ③ 구글 앱스 스크립트 (`Code.gs`) 추가 코드
```javascript
function doGet(e) {
  // ... 기존 action 분기 코드 아래에 추가
  if (action === 'getClassReport') {
    return getClassReportData(e.parameter.date, e.parameter.room);
  }
}

function getClassReportData(dateStr, roomName) {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var studentSheet = ss.getSheetByName("학생명단");
  var logSheet = ss.getSheetByName("Attendance_Log");
  
  // 1. 학생명단에서 해당 반에 배정된 학생 목록 추출
  // 2. Attendance_Log에서 해당 날짜, 반의 출결 로그 추출
  // 3. 두 데이터를 학번 기준으로 결합하여 JSON 반환
  // ... (상세 로직 구현 예정)
}
```
